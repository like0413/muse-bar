{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bar",
  "description": "Default permissions for the taskbar Bar window",
  "windows": ["bar"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
